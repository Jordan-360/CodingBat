WarmUp-1 : makes10

Given 2 ints, a and b, return true if one if them is 10 or if their sum is 10.

public boolean makes10(int a, int b) {
//checking if a OR b is equal to 10 OR a + b == 10
  if((a == 10 || b == 10) || (a + b == 10)) { 
  //if so, we return true
    return true;
  } else { 
  //if not, we return false
    return false;
  }
}
