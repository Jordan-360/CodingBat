Warm-Up-1 : in1020

Given 2 int values, return true if either of them is in the range 10..20 inclusive.

public boolean in1020(int a, int b) {
//Checking if a is greater than or equal to 10 & a is also less than or equal to 20 OR b is greater than or equal to 10
& b is also less than or equal to 20
 if((a >= 10 && a <= 20) || (b >= 10 && b <= 20)) {
            return true;
        } else {
            return false;
        }
}
