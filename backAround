WarmUp-1 : backAround

Given a string, take the last char and return a new string with the last char added at the front and back, so "cat" yields "tcatt". 
The original string will be length 1 or more.

public String backAround(String str) {
//create a new string variable and set it to str
   String str2 = str;
   //return the charactrer in str2 at the index of the length of str - 1 + the substring of str2 from index 0 to the length of str + the character from str at the index of the length of str - 1
        return str2.charAt(str.length() - 1) + str2.substring(0, str.length()) + str.charAt(str.length() - 1);
}


